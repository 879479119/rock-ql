const traverse = (ast: any, options: any) => {
  return ast
}

export default traverse
//
// export const cutNode = (root: any, node: any) => {
//   let visitor = root
//   while(visitor) {
//
//     visitor = visitor.
//   }
// }
